# Copy to server/.env (the setup script does this automatically if .env is missing).
# Values come from your Supabase project → Settings.

# Database → Connection string
#   DATABASE_URL: the POOLED connection (Transaction mode, port 6543) + ?pgbouncer=true
#   DIRECT_URL:   the DIRECT connection (Session mode, port 5432), used for migrations
DATABASE_URL="postgresql://postgres.PROJECT:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.PROJECT:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres"

# Your Supabase project URL, e.g. https://abcdefgh.supabase.co
# (used to verify auth tokens signed with asymmetric keys)
SUPABASE_URL="https://YOUR-PROJECT.supabase.co"

# Settings → API → JWT Settings → JWT Secret (verifies tokens signed with the legacy secret)
SUPABASE_JWT_SECRET="your-supabase-jwt-secret"

# A long random string you generate yourself — signs the staff-tablet session token.
TABLET_JWT_SECRET="change-me-to-a-long-random-string"

# Local dev only.
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"
